#include "jump_op.hpp"

enum class Scope {Local};
enum class DecayType {SigmaMinus, SigmaPlus, Dephasing};

std::vector<Complex> constructJumpOperator(int num_states, Scope scope, DecayType type){

    switch(scope)
    {
    if (type == DecayType::SigmaMinus){

    };
    if (type == DecayType::SigmaPlus){
        break;

    };
    if (type == DecayType::Dephasing){
        break;
    };

    };
   
}